// eslint-disable-next-line no-shadow
export enum OrderStatus {
  PENDING,
  IN_PROGRESS,
  DONE,
  ABORTED,
  ON_REWORK,
}
// eslint-disable-next-line no-shadow
export enum MasterStatus {
  IDLE,
  BUSY,
  HOLIDAY,
}
